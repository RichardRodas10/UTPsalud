<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">

    <!-- El fondo con radios redondeados -->
    <item android:drawable="@drawable/contenedor_fragmenthome" />

    <!-- Máscara para que el ripple siga el mismo contorno redondeado -->
    <item android:id="@android:id/mask"
        android:drawable="@drawable/contenedor_fragmenthome" />
</ripple>
